[project]
name = "dev-workspace"
version = "0.1.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["tools/mono-switch", "tools/pi-extensions-update"]

# Ruff baseline lives in ruff.toml (buildPythonProject passes it via
# --config, which needs a pure ruff config file).
