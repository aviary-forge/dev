[project]
name = "email-classifier-engine"
version = "0.1.0"
description = "Classification engine"
readme = "README.md"
authors = [
  { name = "Denbeigh Stevens", email = "denbeigh@denbeighstevens.com" },
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
email-classifier-engine = "email_classifier:main"

[build-system]
requires = ["uv_build>=0.9.30,<0.10.0"]
build-backend = "uv_build"
