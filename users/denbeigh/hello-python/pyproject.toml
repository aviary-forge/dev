[project]
name = "hello-python"
version = "0.1.0"
requires-python = ">=3.12"

[project.scripts]
hello-python = "hello_python:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
