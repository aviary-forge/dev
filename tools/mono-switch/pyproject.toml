[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mono-switch"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
mono-switch = "mono_switch:main"
